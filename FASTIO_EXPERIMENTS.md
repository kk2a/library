# fastio 実験記録

測定環境: `g++ -std=c++20 -O2 -pipe`、入力は library-checker の
`sample/many_aplusb` 公式生成データ、CPU 0 固定。時間は特記しない限り
同一入力を複数回実行した合計値。

## これまでの実験

| 実験 | 結果 | 採否 |
|---|---|---|
| SWAR 8/4/2 桁入力 | 旧版より大幅に高速 | 採用 |
| 出力 4 桁 LUT | 出力コストを大幅削減 | 採用 |
| 逆数乗算による `10^N` 除算 | 13 桁出力: 約9.8ms/run → 約5.3ms/run | 採用 |
| `std::copy_n` による LUT コピー | 出力単体で約8%改善 | 採用 |
| 出力終端ポインタのキャッシュ | 追加で数%改善 | 採用 |
| 入力先読み閾値 64 → 32 | 安全だが遅い | 不採用 |
| 入力先読み閾値 64 → 16 | 境界をまたぐ長い数値で実際に越境 | 不採用 |
| 入出力バッファ 64/256 KiB | 安定した改善なし | 不採用 |
| `fread_unlocked` / `fwrite_unlocked` | この環境では遅い | 不採用 |
| GCC `Ofast` / `unroll-loops` pragma | ケースにより悪化 | 不採用 |
| `alignas(64)` | 改善なし、ケースにより悪化 | 不採用 |

## 公開入力での最終確認

現在版と参考実装を各30回実行した概算（1回あたり、ms）。

| ケース | 現在版 | 参考実装 |
|---|---:|---:|
| `random_00` | 8.7 | 8.7 |
| `random_01` | 10.0 | 10.3 |
| `digit_random_00` | 24.3 | 24.3 |
| `digit_random_01` | 24.3 | 25.3 |
| `max_random_00` | 19.0 | 21.0 |
| `max_random_01` | 22.0 | 19.3 |
| `all_max_00` | 17.7 | 17.0 |

## 追加実験

### 先読み閾値 64 → 48 バイト

安全性テストは通過したが、公開入力ではおおむね5〜10%遅くなった
（例: `max_random_00` は約18.8ms/run → 約20.5ms/run）。不採用。

### 出力変換関数への `always_inline`

`put_leading_block` / `put_padded_blocks` / `put_u64_blocks` に付与した。
公開入力で random は約0.47〜0.52秒/50回、`all_max` は約0.89〜0.94秒/50回となり、
通常版より悪化。コードサイズ増加の影響とみられ、不採用。

### コンパイルオプション `-O2` → `-O3`

公開入力50回の比較では、`random_00`: 0.45→0.46秒、
`max_random_00`: 0.99→1.02秒、`digit_random_00`: 1.35→1.33秒、
`all_max_00`: 0.87→0.87秒。測定誤差の範囲で、一貫した効果なし。

### 入力バッファ 128 → 256 KiB（出力は128 KiBのまま）

`random_00` は約0.46秒、`max_random_00` は約0.99秒、
`digit_random_00` は約1.35秒、`all_max_00` は約0.81秒/50回で、
ケース間の揺れを超える一貫した改善なし。不採用。

### `KK2_FASTIO_UNBUFFERED`

stdio側の二重バッファを無効化するオプションを現在版で再測定した。
80回合計で `max_random_00`: 1.60→1.61秒、`random_00`: 0.71→0.70秒、
`all_max_00`: 1.42→1.34秒。入力・出力をこのヘッダだけで扱う場合に限り、
`all_max` のような大量出力では有効。デフォルト動作は変更しない。

### stdioバッファを1 MiBに拡大

一時的に `KK2_FASTIO_LARGE_STDIO_BUFFER` を追加して測定した。
80回合計で `random_00`: 0.70→0.71秒、`max_random_00`: 1.52→1.58秒、
`digit_random_00`: 1.94→1.93秒、`all_max_00`: 1.26→1.27秒。
改善はなく、オプションも削除した。

### 入力SWAR 8桁ループの `unroll 2`

`random_00` と `digit_random_00` では僅かに改善する測定もあったが、
`max_random_00` では悪化し、再測定でも一貫性がなかった。不採用。

### POSIX `read(2)` / `write(2)` バックエンド

`KK2_FASTIO_POSIX` を追加し、stdioを使わずファイルディスクリプタへ直接アクセスする。
部分読み書き、`EINTR`、EOF、エラー時の未送信データ保持を実装した。
60回合計では、`random_00`: 0.54→0.52秒、`max_random_00`: 1.27→1.16秒、
`digit_random_00`: 1.63→1.46秒、`all_max_00`: 1.01→0.92秒で、約8〜11%高速化。
POSIX環境でこのヘッダだけが対象ストリームを使う場合の選択肢として採用し、
デフォルトのstdio経路は変更しない。

同じ条件で参考実装とも比較すると、60回合計で `random_00`: 0.53秒対0.48秒、
`max_random_00`: 1.15秒対1.10秒、`digit_random_00`: 1.46秒対1.42秒、
`all_max_00`: 0.92秒対0.93秒となった。

なお、POSIX版の部分書き込み処理は、書き込めたバイト数を保持してエラー時に残りだけを
バッファへ戻すよう修正した。修正後も警告ビルドとASan/UBSanを通過している。

### AVX2による16桁先行判定

`-DKK2_FASTIO_AVX2 -mavx2` で最初の16バイトがすべて数字かをベクトル判定し、
該当時だけSWARを2ブロック先行処理した。短い数値では判定コストが大きく、
100回測定でも `random` はスカラー版0.88〜0.89秒に対して0.91〜0.98秒、
`max_random` は1.99〜2.11秒に対して2.03〜2.40秒となった。分布依存の揺れも大きく、不採用。

### ファイル全体の一括読み込み

正規ファイルを `pread` で一度に読み込み、追加バッファ上で処理する方式を試した。
1回実行でも `max_random_00`: 約20→30ms、`all_max_00`: 約10→30msとなり、
初期確保・コピー・状態分岐のコストが大きかった。不採用。

### `mmap` + 終端番兵ページ

正規ファイルを直接マップし、隣接する匿名ページを番兵領域にする方式を試した。
`random_00`: 0.52→0.50秒以外は同等〜悪化し、さらに既存APIの `eof()` 状態が
一致しないことを単体テストで検出した。速度・互換性の両面から不採用。

### 4桁LUTを32ビット整数表へ再配置

リトルエンディアン時に4バイトを一括ロードする表を追加したが、
`random_00`: 0.50→0.52秒、`max_random_00`: 1.14→1.21秒、
`digit_random_00`: 1.46→1.49秒、`all_max_00`: 0.93→0.95秒/60回で悪化。
文字表＋`copy_n`を維持する。
